function trimean()
% Run 'pkg load nan' to enable this function.
%  TRIMEAN yields the weighted mean of the median and the quartiles m = TRIMEAN(y).
  fprintf(stderr, "Run 'pkg load nan' to enable this function.\n");
  fprintf(stderr, " TRIMEAN yields the weighted mean of the median and the quartiles m = TRIMEAN(y).\n");
endfunction
