function histo()
% Run 'pkg load tsa' to enable this function.
%  HISTO calculates histogram for each column [H,X] = HISTO(Y,Mode) Mode 'rows' : frequency of each row '1x' : single bin-values 'nx' : separate bin-values for each column X are th
  fprintf(stderr, "Run 'pkg load tsa' to enable this function.\n");
  fprintf(stderr, " HISTO calculates histogram for each column [H,X] = HISTO(Y,Mode) Mode 'rows' : frequency of each row '1x' : single bin-values 'nx' : separate bin-values for each column X are th\n");
endfunction

