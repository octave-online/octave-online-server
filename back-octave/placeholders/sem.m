function sem()
% Run 'pkg load nan' to enable this function.
%  SEM calculates the standard error of the mean [SE,M] = SEM(x [, DIM [,W]]) calculates the standard error (SE) in dimension DIM the default DIM is the first non-single dimension M returns 
  fprintf(stderr, "Run 'pkg load nan' to enable this function.\n");
  fprintf(stderr, " SEM calculates the standard error of the mean [SE,M] = SEM(x [, DIM [,W]]) calculates the standard error (SE) in dimension DIM the default DIM is the first non-single dimension M returns \n");
endfunction

