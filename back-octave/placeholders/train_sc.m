function train_sc()
% Run 'pkg load nan' to enable this function.
%  Train a (statistical) classifier CC = train_sc(D,classlabel) CC = train_sc(D,classlabel,MODE) CC = train_sc(D,classlabel,MODE, W) weighting D(k,:) with weight W(k) (not all classifiers suppo
  fprintf(stderr, "Run 'pkg load nan' to enable this function.\n");
  fprintf(stderr, " Train a (statistical) classifier CC = train_sc(D,classlabel) CC = train_sc(D,classlabel,MODE) CC = train_sc(D,classlabel,MODE, W) weighting D(k,:) with weight W(k) (not all classifiers suppo\n");
endfunction

