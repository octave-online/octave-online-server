function rc2ar()
% Run 'pkg load tsa' to enable this function.
%  converts reflection coefficients into autoregressive parameters uses the Durbin-Levinson recursion for multiple channels function [AR,RC,PE,ACF] = rc2ar(RC); function [MX,PE] = rc2ar(RC);
  fprintf(stderr, "Run 'pkg load tsa' to enable this function.\n");
  fprintf(stderr, " converts reflection coefficients into autoregressive parameters uses the Durbin-Levinson recursion for multiple channels function [AR,RC,PE,ACF] = rc2ar(RC); function [MX,PE] = rc2ar(RC);\n");
endfunction

