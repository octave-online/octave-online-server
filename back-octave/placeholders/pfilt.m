function pfilt()
% Run 'pkg load ltfat' to enable this function.
% PFILT Apply filter with periodic boundary conditions. Usage: h=pfilt(f,g); h=pfilt(f,g,a,dim);
  fprintf(stderr, "Run 'pkg load ltfat' to enable this function.\n");
  fprintf(stderr, "PFILT Apply filter with periodic boundary conditions. Usage: h=pfilt(f,g); h=pfilt(f,g,a,dim);\n");
endfunction

