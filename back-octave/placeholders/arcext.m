function arcext()
% Run 'pkg load tsa' to enable this function.
%  ARCEXT extracts AR and RC of order P from Matrix MX function [AR,RC] = arcext(MX,P);
  fprintf(stderr, "Run 'pkg load tsa' to enable this function.\n");
  fprintf(stderr, " ARCEXT extracts AR and RC of order P from Matrix MX function [AR,RC] = arcext(MX,P);\n");
endfunction

